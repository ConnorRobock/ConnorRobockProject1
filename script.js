// Pseudo Code - Nav dropdown menu

// Identify element we will need to listen too


// Could not get menuIcon to display: block;
const slideOutButton = document.getElementsByClassName("slideOutNav");
const menuIcon = document.querySelector(".menuIcon");

menuIcon.addEventListener("click", function(event) {
    console.log("clicked");
});